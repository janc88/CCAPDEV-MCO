import { SearchContainer, SearchIcon } from "./SearchBarResults.styled";

const SearchResultsBar = () => {
	return (
		<SearchContainer>
            <SearchIcon/>
			Search results for "text"
		</SearchContainer>
	);
};

export default SearchResultsBar;